# Smoke test — app launches and shows the expected initial state.
# Tier-3 criterion: pre-state (Soul Piece #1's "fullscreen conversation").
appId: com.matthewharrilal.dotpinchprototype
tags:
  - smoke
---
- launchApp:
    clearState: true
- waitForAnimationToEnd
- assertVisible:
    id: "ConversationSurface"
- assertVisible:
    id: "TodaySlotGuide"
- assertVisible:
    id: "StatusLabel"
- takeScreenshot: 01_initial_state
