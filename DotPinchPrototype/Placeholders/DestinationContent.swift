// Static content for the destination card (the "memory" the conversation
// becomes once the morph completes).

enum DestinationContent {
    static let date = "Mon, Jul 1"
    static let preview = "Good morning check-in, daily exercise nudge, 20-minute workout suggestions"
    static let composerHint = "Share with Dot…"
}
